async function deletarProduto(id) {
    window.confirm('Você deseja excluir esse produto? Essa ação é permanente')

    /*try {
        const resposta = await fetch(`https://localhost:7288/Produto/${id}`, {
            method: 'DELETE'
        })

        if (resposta.ok) {
            window.alert('Produto excluido com sucesso!')

            listarProdutos()
        } else {
            window.alert('O servidor C# recebeu, mas retornou um erro.')
        }
        } catch (erro) {
            console.error('Erro de rede: A API pode estar desligada ou fora do ar.', erro)
        }*/;

        
}